# Engineering Journal — 2026-09

Append-only journal of completed meaningful engineering tasks. Exact diffs remain in Git history.

## 2026-09-03 · T3 - Background polling cadence

### Problem

Фіксований ~15s API-полер означав, що приховані/background клієнти також опитували API кожні ~15 секунд. Поточна затримка не могла бути скинута при переході режиму; не існувало спільного негайного poll для restore / Check now; негайне опитування мало зберігати одноразову серіалізацію запитів та семантику паузи операції; фінальний concurrency-рев'ю виявив витік власності linked scheduler-wait CTS у першій реалізації.

### Decision

- Збережено один `ReleaseFeedPoller.RunLoopAsync`; додано `ReleaseFeedPollingMode.Visible / Background`; виробничі інтервали 15s / 5min; стан планувальника захищено `_schedulerLock`.
- Скасовний поточний scheduler-wait через `_schedulerWaitCts`; додано `SetPollingMode`, `RequestImmediatePoll`; `PerformPollAsync` — єдиний recurring/immediate шлях API-запиту (`_apiClient.GetReleasesAsync`).
- Зміна режиму скидає поточну затримку без власного poll; restore = Visible + негайний poll; Check now використовує негайний poll без restore.
- Кілька негайних коалесують; активний запит задовольняє суміжний негайний (Option A); Pause авторитетний; Resume запускає свіжу каденцію поточного режиму.
- `RunLoopAsync` володіє кожним linked wait CTS і звільняє його у `finally` через `ReleaseSchedulerWait`; `Dispose` не володіє per-wait CTS.

### Changed

Runtime/tests:

- `Services/ReleaseFeedPoller.cs`
- `MainForm.Tray.cs`
- `BdoClient.Tests/Services/ReleaseFeedPollerTests.cs`

Документація:

- `docs/plans/README.md`
- `docs/plans/active/background-tray-notifications.md`
- `docs/plans/active/code-quality-ux-improvements.md`
- `docs/development/CURRENT.md`
- `docs/development/history/2026-09.md` (новий)

### Validation

- Release build: 0 warnings / 0 errors.
- 862 passed / 0 failed (baseline 845 + 13 scheduler/cadence/immediate/pause тестів + 4 додаткових CTS-ownership регресійних тести).
- 13 scheduler/cadence/immediate/pause тестів; 4 додаткових CTS-ownership регресійних тести.
- Покриття max API concurrency <= 1.
- Фінальний scheduler-ownership рев'ю: PASS.
- Статичний MainForm tray-integration рев'ю: PASS.
- Живого tray/runtime E2E у цьому кроці не виконувалось.
- Без змін Registry/config/стану файлів гри.
- Без поведінки T4/T5.

### Consequences

- Видимий клієнт використовує ~15s API каденцію.
- Background клієнт використовує ~5min API каденцію.
- Restore/Check now можуть оновити API негайно без другого шляху poll.
- Резидентний процес більше не витікає per-iteration linked scheduler CTS об'єктами.
- Наступне: T4 — Local file-change trigger.
- Stage B лишається відкладеним.

### Follow-up

`T4 — Local file-change trigger`

## 2026-09-04 · T4 - Local file-change trigger

### Problem

API-фід реагує лише на семантичну зміну релізу; гра/ланчер може перезаписати `ads\languagedata_en.loc`, поки API-фід не змінився, тому API-полінг сам не виявляє локальну заміну. Постійне SHA-хешування для резидентного tray-клієнта було б марнотратним. Потрібен дешевий метаданний тригер, чий lifecycle переживає приховані операції та тимчасові збої capture, не помилково моніторячи власні транзакційні записи.

### Decision

- Окремий `System.Windows.Forms.Timer` ~5 хв, яким володіє `MainForm.LocalFileMonitor.cs`; `ReleaseFeedPoller` не змінено (T3 ізольовано).
- Відбиток `LocalizationFileFingerprint`: `Exists` / `Length` / `LastWriteTimeUtc`; відсутній файл — канонічний валідний відбиток; IO-помилки capture тимчасові й ретраяться без просування baseline.
- `LocalFileChangeTracker` — лише RAM, зі зіставленням шляху `OrdinalIgnoreCase`; baseline = відбиток, пов'язаний з останнім успішним `RefreshStateAsync`.
- `RefreshStateAsync` захоплює відбиток ДО `ResolveAsync` і комітить його (лише для валідного API-managed стану) після повної успішної презентації; без пост-резолюційного перезахоплення (інваріант F0/F1/F2).
- Незмінений відбиток → без `RefreshStateAsync`/SHA; змінений/відсутній baseline → існуючий шлях `RefreshStateAsync` (без тихого `CommitResolved(current)`).
- `FeedApplicationCoordinator` серіалізує локальну й API-реконсиляцію: `BlockUpdates()` до першого await, `await RefreshStateAsync()`, `await ApplyPendingIfAnyAsync()`, `UnblockUpdates()` у `finally`.
- Restore виконує одне дешеве порівняння через `BeginInvoke` + `RunLocalFileCheckSafeAsync`; `RestoreFromTray` лишається синхронним.
- Таймер може лишатися живим під час прихованої операції, але фактичний checker повертає до зчитування файлу, доки `_operationInProgress`/`IsBlocked`. Перехід у Official/Missing/Invalid/видимий стан зупиняє таймер із збереженням baseline. Реальне завершення → `DisposeLocalFileMonitor()`.

Корекції під час review:

1. початкова реалізація могла залишити монітор зупиненим після прихованих операцій;
2. початкова реалізація не ретраяла, коли baseline не було встановлено;
3. виправлено розділенням таймер-START/liveness від CHECK-безпеки (operation/feed-blocked більше не є умовою зупинки таймера);
4. фінальний review виявив, що таймер лишався увімкненим після API→Official/Missing/Invalid;
5. виправлено перетворенням `StartLocalFileMonitorIfEligible()` на ідемпотентний синхронізатор eligibility (негативна eligibility зупиняє таймер, зберігаючи baseline).

### Changed

Runtime:

- `Services/LocalizationFileFingerprint.cs`
- `Services/LocalFileChangeTracker.cs`
- `MainForm.LocalFileMonitor.cs`
- `MainForm.Localization.cs`
- `MainForm.Tray.cs`

Tests:

- `BdoClient.Tests/Services/LocalizationFileFingerprintTests.cs`
- `BdoClient.Tests/Services/LocalFileChangeTrackerTests.cs`

Docs:

- `docs/plans/README.md`
- `docs/plans/active/background-tray-notifications.md`
- `docs/plans/active/code-quality-ux-improvements.md`
- `docs/development/CURRENT.md`
- `docs/development/history/2026-09.md`

### Validation

- Release build: 0 warnings / 0 errors.
- 880 passed / 0 failed (baseline 862 + 18 new T4 тестів: 9 fingerprint + 9 tracker).
- Temp-directory/file тести; покриття F0/F1/F2; case-insensitive Windows path покриття.
- Статичний lifecycle-рев'ю: прихована операція, self-update staging failure, no-baseline recovery, restore, API/feed serialization, shutdown, API→Official зупинка таймера.
- Без модифікації реального файлу гри; без реального Install/Restore T4 E2E; без поведінки T5.

### Consequences

- Резидентний процес виявляє заміну грою/ланчером без семантичної зміни API.
- Незмінений файл коштує лише низькочастотної перевірки метаданих.
- SHA рахується лише через існуючий state resolution після фактичної зміни відбитку/no-baseline реконсиляції.
- Наступне: T5 — Notifications / dedup.
- T6 лишається власником фінального owner/runtime/resource E2E.
- Stage B лишається відкладеним.

### Follow-up

`T5 — Notifications / dedup`

## 2026-09-04 · T5 - Notifications / dedup

### Problem

- Background client could detect an update without informing the user.
- Repeated polling could spam notifications.
- Notification failure must not break feed/state correctness.

### Decision

- `UpdateAvailable`-only actionability with a RAM-only episode latch.
- Visible observations latch without a balloon; the first hidden episode notifies.
- Non-actionable factual state resets the episode.
- No PublicId/mode dedup, persistence, scheduler, or dependency.
- Generic balloon text; click reuses `RestoreFromTray()`; `ShowBalloonTip` is isolated.

### Changed

- Runtime, pure tests, plans, registry, CURRENT, journal, and §39 repository rules.

### Validation

- Release build: 0 warnings / 0 errors.
- 891 tests passed / 0 failed.
- Static notification integration checks passed.

### Consequences

- Background users can be informed without repeated notifications for one unresolved episode.
- A process restart may notify once again.
- Real Windows visual/click E2E is deferred to T6; T5 remains pending architect review.

### Follow-up

- Architect T5 review, then T6 Owner E2E / resource validation.

## 2026-09-04 · T5/T6 validation resolution

### Problem and investigation

- Native `NotifyIcon.ShowBalloonTip` appeared with the approved title and body, but genuine activation did not deliver WinForms `BalloonTipClicked` on the current Windows notification surface.
- The event was not delivered; `RestoreFromTray()` was never entered. Guards and the MainForm lifecycle were therefore not responsible.

### Decision and change

- The native notification remains informational only; click-to-restore is intentionally outside the first-version contract.
- The unreliable `BalloonTipClicked` subscription was removed. No generic `Click`/`Closed` workaround, Windows App SDK, COM activation, or new dependency was introduced.
- Tray `Відкрити`, tray double-click, and secondary-instance activation remain the canonical restore paths.

### Validation

- T6 Windows UI: X → tray, Open, double-click, Exit, secondary instance, notification appearance, and notification dedup passed.
- Headless background startup, duplicate background instance, approximately 300.7-second cadence, resource sanity, T4 monitor behavior, and process cleanup passed.
- Release build: 0 warnings / 0 errors; 891 tests passed / 0 failed; diff check passed.

### Consequence and follow-up

- T5 and T6 are COMPLETED / REVIEWED / ACCEPTED. The tray/background feature is ready for release preparation.
- A richer actionable Windows toast may be considered separately in the future; it is not part of the current plan.

## 2026-09-05 · Release preparation v1.2.0

- Архітектор прийняв T1–T6 background-tray-notifications; публічний README та release-facing плани синхронізовано з фінальною інформаційною notification-контрактною поведінкою.
- Release preflight для явної версії `1.2.0` підтвердив monotonic target після v1.1.3; build, tests і статичну перевірку pipeline виконано.
- Наступна дія: Release Candidate v1.2.0. Stage B залишається відкладеним до завершення релізу.

## 2026-09-05 · v1.2.0 published / tray plan archived

- RC #27 успішно завершився; стабільний v1.2.0 опубліковано на tag `v1.2.0`.
- Live bundle містить рівно один asset `BDO-UA-Client-v1.2.0-win-x64.zip`; asset digest: `sha256:2919558b27d708c9faf90d5016c445e015f3c0c6ef8daba58623aaec5279db41`.
- Internal EXE SHA-256: `62a7464660a50a8cfdf88f8f765876dba9c70f4ca8e2a73c348772bcac7c5d20`.
- Tray/background T1–T6 release lifecycle завершено; `background-tray-notifications` фізично переміщено до archive.
- PRIMARY `code-quality-ux-improvements` resumed; наступна фаза — Stage B read-only audit перед реалізацією. У post-release task runtime не змінювався.

## 2026-09-05 · Stage B.1 — canonical raw installation-state capture

- Додано `InstallationStateStore.CaptureRawState()` як єдину синхронну операцію захоплення raw bytes canonical `installation.json`.
- `null` зберігає семантику відсутнього файлу, наявний zero-length файл повертається як non-null `byte[0]`, arbitrary/malformed bytes не десеріалізуються й повертаються без змін.
- Три дубльовані service-local `ReadRawInstallationState()` видалено; п’ять call sites переведено на store primitive. Порядок transaction flow не змінено.
- Raw write/restore/delete, rollback logic, cancellation behavior, restore-point format і schema не змінювалися.
- Validation: Release build — 0 warnings / 0 errors; full Release test suite — 894 passed / 0 failed; `git diff --check` — passed.
- B.1 очікує architect review. Наступна дія: `Architect review B.1, then B.2 canonical raw restore`.

## 2026-09-06 · Stage B.2 — canonical raw installation-state restore

- B.1 externally reviewed/accepted; додано `InstallationStateStore.RestoreRawStateAsync(byte[]?, CancellationToken)` як canonical primitive для mutation canonical `installation.json`.
- `null` відновлює absent state з idempotent delete/verification; non-null bytes, включно з `byte[0]`, відновлюють present state через unique state-directory temp file, atomic `File.Replace`/`File.Move` і exact byte verification.
- Caller token використовується для temp write та verification read; explicit cancellation checkpoint між temp write і Replace/Move не додано; primitive не виконує rollback і не змінює error propagation.
- Додано шість прямих тестів для absent/present/empty/Replace/Move/cancellation semantics. Service migrations навмисно відкладено до B.3.
- Validation: Release build — 0 warnings / 0 errors; full Release test suite — 900 passed / 0 failed; `git diff --check` — passed.
- B.2 очікує architect review. Наступна дія: `Architect review B.2, then B.3 raw-state call-site migration`.

## 2026-09-06 · Stage B.3 — raw installation-state service migration

- B.2 externally reviewed/accepted; мігровано три canonical installation-state mutation surfaces на `InstallationStateStore.RestoreRawStateAsync`.
- `LocalizationInstallService` install/update rollback тепер використовує `CancellationToken.None` для state recovery та зберігає окремі game/state rollback success dimensions.
- `RestoreBackupService` selected restore-point state apply використовує caller operation token; pre-operation state rollback використовує `CancellationToken.None`. Existing error mapping, rollback ordering і OCE propagation збережені.
- Видалено service-local raw temp/write/Replace/Move/Delete/verification для canonical `installation.json`. `SaveAsync`, `BackupStore` restore-point snapshots і transaction orchestration залишилися окремими за дизайном.
- Додано один integration test для present zero-length restore-point state; legacy marker/classification і selected-state read не змінювалися.
- Validation: Release build — 0 warnings / 0 errors; full Release test suite — 901 passed / 0 failed; `git diff --check` — passed.
- B.3 очікує architect review. Наступна дія: `Architect review B.3, then close Stage B and proceed to Stage D responsiveness investigation`.

## 2026-09-06 · Stage E.1 — game patch status presentation

- B.3 externally accepted; Stage B закрито зі статусом **COMPLETED / REVIEWED / ACCEPTED**.
- D.1 externally accepted: representative UI-thread local IO виміряно як negligible; D.2 — **NOT REQUIRED**, async conversion не виправдана.
- E.2 підтверджено як **NO ACTION REQUIRED / ALREADY SATISFIED**: `LocalizationModeCard` уже має hover surface/border feedback.
- E.1 реалізовано в централізованому `SetGameFound`: позитивний patch з `AdsFilesPatchReader.TryReadPatch` додається до automatic/manual status; unavailable або invalid patch зберігає попередній game-found text.
- Validation: Release build — 0 warnings / 0 errors; full Release test suite — 901 passed / 0 failed; `git diff --check` — passed.
- Наступна дія: `Architect review E.1, then close code-quality-ux-improvements plan`.

## 2026-09-06 · v1.2.1 release handoff

- E.1 externally accepted; full `code-quality-ux-improvements` roadmap considered **COMPLETED / REVIEWED / ACCEPTED**. E.2 remains **NO ACTION REQUIRED / ALREADY SATISFIED**.
- CI #171 succeeded on preparation commit `fad8d7ab2be3f313e3bac58b2c2d4b09aeb83388`.
- Factual-state duplication between install preflight and general refresh explicitly **DEFERRED / NO CURRENT ACTION**: responsibilities and lifecycles differ, and no shared abstraction is introduced without a concrete defect or separately approved focused task.
- Release target selected: `v1.2.1`; stable public release remains `v1.2.0`. PRIMARY plan remains active for release handoff.
- Next action: `Release Candidate v1.2.1`.

## 2026-09-06 · v1.2.1 published / R1 release-experience-polish

- Stable `v1.2.1` опубліковано на tag `v1.2.1` (release ID `383553345`); RC #28 / run `34028489675` і normal CI #172 успішні. Public asset — один `BDO-UA-Client-v1.2.1-win-x64.zip`; outer SHA-256 `e3dadacc2284a8b60db8c8fcda5e29a175f89a77b5aa2d0d587f81d523c65ba8`, internal EXE SHA-256 `6b91632c3dda7508ae41aa8c9b9b335f2f13d593e08e0e3a1978d5bc95455aac`.
- Власник успішно виконав реальне вбудоване оновлення `v1.2.0 → v1.2.1` у production. Після релізу виявлено, що довгоживучий tray-процес не виконував application-update discovery після startup; root cause — startup-only discovery.
- Реалізовано R1: один 5-хвилинний application-update timer, single-flight request path, hidden per-tag RAM-only notification, immediate discovery після restore і через tray `Перевірити зараз`; automatic download/install не додано.
- `code-quality-ux-improvements` переведено до archive як завершений, reviewed/accepted plan. Створено новий ACTIVE PRIMARY `release-experience-polish`; R2 deterministic structured release notes залишено NOT IMPLEMENTED.
- Validation: Release build, full tests і `git diff --check` пройдені; `ReleaseFeedPoller`, `GitHubUpdateClient`, `UpdateSelectionPolicy` та persisted schemas не змінювалися.
- Наступна дія: `Architect review R1, then R2 deterministic structured release notes`.

## 2026-09-06 · R2 — deterministic structured release notes

- R1 externally reviewed/accepted; stale `AGENTS.md` §41.2 startup-only self-update wording corrected to the accepted resident ~5-minute discovery contract.
- Commit-log heuristic release-note generation replaced with schema-v1 `docs/releases/NEXT.json`; generator now performs deterministic schema, type, blank-item and internal-token validation and renders fixed Ukrainian sections/boilerplate without LLM, API or dependencies.
- Added standalone generator tests to normal CI; Release Candidate workflow now consumes the structured source explicitly while preserving the existing four-file artifact and manual GitHub Release publication.
- `NEXT.json` seeded with concise user-facing application-update notes for future v1.2.2. Validation passed: Release build/tests, resolver tests, generator tests, real generator smoke and `git diff --check`.
- Наступна дія: `Architect review R2, then R3 Release Candidate v1.2.2`. R3 не розпочато.

## 2026-09-06 · R3 — release preparation v1.2.2

- R2 externally reviewed/accepted after the focused Release Candidate workflow-summary correction; structured generator and `NEXT.json` contract remain unchanged.
- Target `v1.2.2` confirmed; `NEXT.json` public copy reviewed and retained without reset. Explicit resolver returned `RELEASE_VERSION=1.2.2` and `RELEASE_TAG=v1.2.2`; tag was absent locally and remotely.
- Release preflight passed: Release build 0 warnings / 0 errors, 907 tests passed / 0 failed, resolver tests, generator tests, structured-note smoke and `git diff --check`.
- R3 entered release handoff. Next: `Release Candidate v1.2.2, then packaged validation and owner publication`.

## 2026-09-06 · v1.2.2 published / release-experience-polish archived

- RC #29 succeeded; stable `v1.2.2` опубліковано на tag `v1.2.2` (release ID `383636389`) з commit `8bad1b700058c7f82666fcf4ac4b1507fb06aa27`.
- Public asset `BDO-UA-Client-v1.2.2-win-x64.zip` має size `67,901,764` bytes і outer SHA-256 `329c31987955dbb2139a061ea09bbad0e89fa403cf731343d124b917e68f120c`; internal EXE SHA-256 `05f0e9214c65c8e013a0162105a8e6a30c6bb201e7b8d99f3ee74cb152cf07ee`.
- Structured generated release body успішно використано в RC; owner built-in update до `v1.2.2` завершився успішно, оновлений застосунок працює. Окреме hidden periodic-notification observation у production smoke не заявляється.
- `NEXT.json` скинуто до порожнього schema-v1 джерела; R1/R2/R3 зафіксовано як **COMPLETED / REVIEWED / ACCEPTED**; `release-experience-polish` фізично переміщено до archive.
- Lifecycle rule уточнено: нуль ACTIVE планів валідний без approved executable roadmap; placeholder PRIMARY не створювався. Repository входить в idle state.
- Наступна дія: `Await explicit owner decision for the next roadmap/task`.

## 2026-09-06 · AI development workflow documentation

- Repository is already post-v1.2.2 closure and idle; full multi-agent orchestration existed operationally but was distributed across rules, conventions and external coordination.
- Owner authorized formalization of canonical vendor-neutral `docs/ai-workflow/`; documented Owner / Architect-Analyst-Reviewer / Implementation Agent authority boundaries, prompt/review/handoff contracts, severity and external acceptance rules.
- Session bootstrap and repository source-of-truth rules are now explicit; `AGENTS.md §43`, stale `§34.2` pointer, development navigation and `docs/index.md` were synchronized.
- No runtime, release, plan or artifact behavior changed. Next: `Architect review AI workflow documentation, then define the next product-development roadmap/task`.

## 2026-09-06 · AI workflow consistency correction

- External Architect review of v15.35/v15.36 identified three documentation consistency defects.
- Portable Architect-chat rules were added in v15.36; this correction restores §42/§43 ordering, adds the explicit prompt Baseline Contract, and corrects CURRENT’s canonical stable-release reference to `docs/releases/v1.2.2.md`.
- No runtime, plan or release behavior changed. Next: external Architect re-review, then define the next product-development roadmap/audit.

## 2026-09-06 · AI workflow: паста-ліміт, докази валідації, дані й межі репозиторіїв

- Зворотний звʼязок із застосування цього ж флоу в іншому проєкті (Localization Hub, `merelyigor/game-ukrainizatory-hub`, `docs/ai-workflow/UPSTREAM-PROPOSALS.md`).
- Попередня версія `PROJECT_CHAT_RULES.md` перевищувала ліміт поля project instructions і супроводжувалася скороченою альтернативною копією для вставки; повна версія лишалася repository-side.
- Report contract: exact validation тепер означає команду й exit code, а розділ «що НЕ зроблено і чому» присутній завжди, навіть коли зроблено все — тихе звуження scope у diff невидиме.
- Evidence hierarchy впорядкована явно: diff → validation output → tests → runtime/CI → і лише потім report.
- Додано prompt-контракт для задач із даними (три числа: було / стало / не звʼязано) і межі за репозиторіями й середовищами в `ROLES.md`.
- Runtime, release і plan behavior не змінювались. Next: `Architect review of the workflow documentation update, then define the next product-development roadmap/task`.

## 2026-09-06 · CI coordinator test-race correction

- Normal CI #180 failed in `FeedApplicationCoordinatorTests.A_SucceedsWithSamePending_PendingCleared_AcceptExactlyOnce` because the test’s mutable `RecordingLogger` List was enumerated while the background poller wrote debug entries.
- External Architect identified the concurrent test-logger List access; production `FileLogger` was not implicated. The test harness now snapshots debug lines under a lock, preserving the exactly-once acceptance assertion.
- No runtime behavior changed. Targeted repeated runs, Release build, and full test validation were performed. Next: Architect review, then read-only product/architecture audit.

## 2026-09-06 · canonical Architect-chat instruction correction

- Defect: repository copy of the Architect-chat instruction diverged from the exact Owner-provided text, and a second shortened instruction copy existed.
- Decision: the exact Owner-provided text is the single canonical copyable instruction. The full file was replaced exactly, the alternate copy was removed, and stale paste-limit references were updated.
- Validation: exact UTF-8 content comparison, reference search, documentation link/scope checks, build and tests. Pending external Architect review.

## 2026-09-08 · v15.41 — MainForm lifecycle integration coverage

- Lifecycle audit confirmed that MainForm composition and event wiring were materially less covered than the existing service/coordinator policies. The bounded test-only strategy was selected; no production seam or architecture redesign was required.
- Added focused STA/message-loop integration coverage for successful startup with saved game state, degraded API startup finalization, secondary activation restoring the same existing MainForm, and shutdown while startup work is pending.
- Tests use unique temporary application data, a temporary fake game root, controlled local HTTP handlers and unique single-instance names; real AppData, Registry, Steam paths, BDO files and external update execution are untouched.
- Validation: Release build — 0 warnings / 0 errors; full Release suite — 911 passed / 0 failed / 0 skipped; targeted suite 4/4 passed; 20/20 independent targeted invocations passed; related lifecycle suites passed. v15.41 remains **IMPLEMENTED / VALIDATED / PENDING EXTERNAL REVIEW**.
- Наступна дія: `External Architect review v15.41 MainForm lifecycle integration coverage`.

## 2026-09-08 · v15.42 — portable client removal guidance

- Added a secondary MainForm link `Як видалити клієнт?` with informational-only MessageBox guidance; it does not modify files, registry, autostart, or game localization.
- Documented portable distribution, tray `Вихід`, autostart disablement, `BDO-UA-Client.exe` removal, optional `%LocalAppData%\\BDO-UA-Client` cleanup, and the separate `Відновити оригінал` action.
- Validation: Release build — 0 warnings / 0 errors; full Release suite — 911 passed / 0 failed / 0 skipped; `git diff --check` passed. v15.42 remains **IMPLEMENTED / VALIDATED / PENDING EXTERNAL REVIEW**.
- Наступна дія: `External Architect review v15.42 portable-client removal guidance`.

## 2026-09-08 · v15.43 — separate RC from final release tag

- External acceptance of v15.41 and v15.42 is recorded; stable public release remains `v1.2.2`.
- Confirmed that the previous Release Candidate workflow created and pushed the final tag automatically after artifact generation. The workflow was corrected to keep the target tag collision check while removing tag creation/push and reducing permissions to `contents: read`.
- RC Summary now identifies the proposed tag and exact source SHA, requires Owner native UI smoke, and states that final tag creation and GitHub Release publication remain outside the RC workflow.
- v15.43 status: **IMPLEMENTED / VALIDATED / PENDING EXTERNAL REVIEW**. The real `v1.2.3` RC was not dispatched; `NEXT.json` and application runtime were not changed.
- Наступна дія: `External Architect review v15.43 release workflow correction, then prepare v1.2.3 Release Candidate`.

## 2026-09-08 · v15.44 — prepare Release Candidate v1.2.3

- v15.43 externally reviewed/accepted; stable public release remains `v1.2.2`.
- Selected patch release `v1.2.3` and populated schema-v1 `docs/releases/NEXT.json` with the user-facing portable-client removal guidance from v15.42.
- Release-prep validation is being performed before the corrected RC workflow dispatch. Final tag and GitHub Release remain intentionally gated by the exact RC artifact and Owner native UI smoke.
- v15.44 status: **IMPLEMENTED / VALIDATED / PENDING EXTERNAL REVIEW**. Offline/degraded work was not started.
- Наступна дія: `Owner native UI smoke of exact v1.2.3 Release Candidate, then final tag/publication after approval`.

## 2026-09-08 · v1.2.3 stable release finalization

- Owner native UI smoke exact RC #30 / run `34236529921` пройдено успішно; annotated tag `v1.2.3` підтверджено на exact approved source SHA `ec891ea7077dd70e2aafd0c2e00674f47c45a94a`.
- Stable GitHub Release `v1.2.3` опубліковано як не-draft і не-prerelease (Release ID `384894464`). Попередня browser upload спроба була заблокована Chrome file chooser; у цій verification-сесії Release уже існував, тому повторне створення не виконувалося. Незалежна перевірка виконана через public GitHub REST API та повторне завантаження asset.
- Public asset `BDO-UA-Client-v1.2.3-win-x64.zip` (asset ID `550865653`) має size `67,904,470` bytes і SHA-256 `0c740be029bfe30a2d020109a817a64af2eb927c24ab143fa868c3fe279718fe`; internal EXE SHA-256 `2abafb502de7f6b8effc8e3ee620afd813ce4ad544fe49d908739e3d4487932b`. ZIP, manifest, version metadata і `SHA256SUMS.txt` збігаються.
- Live self-update eligibility з `v1.2.2` підтверджена: `v1.2.3` чисельно новіший, published stable, має один uploaded canonical bundle asset і не має direct EXE asset.
- Створено `docs/releases/v1.2.3.md`; `docs/releases/NEXT.json` скинуто до порожнього schema-v1 джерела. Offline/degraded implementation не розпочато.
- Наступна дія: `External Architect final release review, then offline/degraded Stage 1`.

## 2026-09-08 · v15.46 — offline/degraded release feed

- Реалізовано bounded offline/degraded mode для release feed: live API залишається authoritative, а валідний normalized last-known snapshot використовується лише для read-only presentation і локального state resolution.
- Cached feed зберігається у cache/release-feed.json з explicit schema version та UTC saved_at_utc; corrupt/unsupported/incomplete cache відхиляється fail-closed, install/update/restore original залишаються Live-only.
- Додано startup fallback, background reconnect transition, best-effort atomic persistence та focused storage/policy/poller/MainForm integration coverage. API, installer safety, GameDetector threading і self-update architecture не змінювалися.
- Validation: Release build — 0 warnings / 0 errors; full Release suite — 924 passed / 0 failed / 0 skipped; focused cache/policy/poller/MainForm suite — 87 passed; five independent MainForm lifecycle invocations passed; git diff --check passed.
- v15.46: **IMPLEMENTED / VALIDATED / PENDING EXTERNAL REVIEW**.
- Наступна дія: External Architect review of offline/degraded release-feed mode.

## 2026-09-08 · v15.46 / v15.47 workflow-cycle closure

- External Architect accepted v15.46 (`1deaacceb999a9a05b03e84fb9da8482b17b8303`) and v15.47; offline/degraded release-feed mode is complete, with CI #189 successful for v15.46.
- `offline-degraded-reliability` was moved from ACTIVE/PRIMARY to archive with all milestones recorded COMPLETED / REVIEWED / ACCEPTED.
- CURRENT and the plan registry now record zero ACTIVE/PRIMARY plans and the terminal state `WORK CYCLE COMPLETE / OWNER DECISION REQUIRED`; no placeholder roadmap or follow-up task was created.
- Next action: explicit Owner decision for the next development cycle.

## 2026-09-08 · v15.49 — portable Architect rules size gate

- Portable `PROJECT_CHAT_RULES.md` скорочено до 3505 UTF-16 code units; деталі залишаються в canonical `AGENTS.md` та `docs/ai-workflow/` документах.
- Додано focused PowerShell gate з hard limit `7500` UTF-16 code units і підключено його до normal CI; позитивна перевірка та негативний over-limit тест пройшли.
- Runtime і release behavior не змінювалися. v15.49: **IMPLEMENTED / VALIDATED / PENDING EXTERNAL REVIEW**; CI #192 успішний для exact SHA `53044774a5245eb26dd4cf837e1c0c8f29b83743`.
- Наступна дія: `External Architect review v15.49 portable rules size gate, then WORK CYCLE COMPLETE / OWNER DECISION REQUIRED`.

## 2026-09-08 · v15.51 — local GitHub CLI access contract

- Repository rules зафіксували, що локально автентифікований GitHub CLI може використовуватися Implementation Agent для task-authorized repository та GitHub Actions operations після перевірки `gh auth status`, target remote і exact SHA/ref.
- Токени й credentials не друкуються, не зберігаються в repository та не потрапляють у handoff; read-only tasks залишаються без мутацій.
- Валідація: repository read і CI read через `gh` успішні; runtime та release behavior не змінювалися.
- v15.51: **IMPLEMENTED / VALIDATED / PENDING EXTERNAL REVIEW**. Наступна дія: `External Architect review v15.49/v15.51 workflow rules, then WORK CYCLE COMPLETE / OWNER DECISION REQUIRED`.

## 2026-09-11 · v15.52 — game patch compatibility status

- Підтверджено UX-проблему: локальний patch гри міг виглядати як ознака готовності, коли release feed вже знав новіший офіційний patch.
- Додано окрему presentation policy: при старішому локальному patch MainForm показує warning із встановленою та актуальною версіями; при невідомому або актуальному patch збережено попередню found-семантику. Localization write-actions у цьому стані не пропонуються як доступні.
- Localization state/compatibility logic, GameDetector architecture, API contract, storage schema та install/restore behavior не змінювалися. Додано focused policy і MainForm lifecycle coverage.
- Validation: focused suite — 19 passed; Release build — 0 warnings / 0 errors; full Release suite — 930 passed / 0 failed / 0 skipped; `git diff --check` passed. v15.52: **IMPLEMENTED / VALIDATED / PENDING EXTERNAL REVIEW**.
- Наступна дія: `External Architect review v15.52 game patch compatibility UX, then WORK CYCLE COMPLETE / OWNER DECISION REQUIRED`.

## 2026-09-11 · v1.2.4 release candidate preparation

- Підготовлено patch release `v1.2.4` для validated game patch compatibility UX fix: застарілий локальний patch порівнюється з актуальним patch release feed, користувачеві показується потреба оновити гру, а localization write-actions блокуються до оновлення.
- `docs/releases/NEXT.json` містить лише два user-facing fix bullets; internal task IDs, workflow details і test/commit prose не додаються до public release notes.
- Збережено чинний RC contract: explicit version resolution, Release build/test, structured release-note generation та no-tag/no-Release workflow. Stable `v1.2.3` залишається опублікованим; фінальні `v1.2.4` tag і GitHub Release ще не створені.
- v1.2.4: **RELEASE PREPARATION / RC PENDING**. Наступна дія: `Release Candidate v1.2.4, then Owner validation and final publication`.

## 2026-09-11 · v1.2.4 stable release publication

- Approved RC #32 / run `34537086177` для source SHA `e2694eb6d4288d7341d11b8bc187ce2a98b1e2de` пройшов Owner native UI smoke; annotated tag `v1.2.4` створено точно на цей SHA.
- Stable GitHub Release `v1.2.4` опубліковано як не-draft і не-prerelease (Release ID `386705757`). Public asset `BDO-UA-Client-v1.2.4-win-x64.zip` (asset ID `556111966`, `67,903,283` bytes) має SHA-256 `6489f11c56da7ff855a0016c9c8362958d3cdabc5216f69f1a93a95b002d9102`; internal EXE SHA-256 `1d75b9b981989a22cbf5a897b293d76a75ad9678f44ffb7004c38603b0d89303`.
- Public ZIP завантажено повторно; чотири flat-файли, manifest schema 2, version metadata і `SHA256SUMS.txt` збігаються. Release body використано зі structured `NEXT.json`.
- `docs/releases/v1.2.4.md` створено, а `docs/releases/NEXT.json` скинуто до порожнього schema-v1 джерела для наступного циклу. v1.2.4: **RELEASED / PUBLIC VERIFIED / PENDING EXTERNAL REVIEW**.
- Наступна дія: `External Architect final release review, then WORK CYCLE COMPLETE / OWNER DECISION REQUIRED`.

## 2026-09-11 · v15.55 — refresh game patch after tray restore and preserve global UI width

- Після відновлення MainForm із tray та після прийняття нового live feed клієнт повторно читає локальний `ads_files` patch і перераховує game presentation, localization state, card presentation та write-action availability без перезапуску.
- Старіший локальний patch блокує localization writes; game patch, новіший за доступну localization, також fail-closed блокує writes; cached/offline feed зберігає display-only semantics. Невдале читання patch не завершує клієнт, а наступне успішне перечитування відновлює нормальний стан.
- Додано lower bound для usable MainForm client width, незалежний від кількості localization cards; runtime layout 1→2→1 і global game/status content захищені focused integration assertions.
- Validation: focused suite — 20 passed; Release build — 0 warnings / 0 errors; full Release suite — 938 passed / 0 failed / 0 skipped; `git diff --check` passed. v15.55: **IMPLEMENTED / VALIDATED / PENDING EXTERNAL REVIEW**.
- Наступна дія: `External Architect review v15.55 hotfix, then prepare Release Candidate v1.2.5`.

## 2026-09-11 · v1.2.5 release candidate preparation

- Після green normal CI для v15.55 підготовлено schema-v1 `docs/releases/NEXT.json` для `v1.2.5` лише з user-facing описом двох hotfix corrections: повторна актуалізація local game patch після tray/live feed та minimum usable width для одного localization mode.
- Internal task IDs, workflow prose і тестові деталі до public release notes не включено; stable public release залишається `v1.2.4`.
- v1.2.5: **RELEASE PREPARATION / RC PENDING**. Наступна дія: `Release Candidate v1.2.5, then Owner native UI smoke of both hotfixes`.

## 2026-09-11 · v1.2.5 stable release publication

- Owner native UI smoke exact RC #33 / run `34550224363` пройдено успішно; annotated tag `v1.2.5` створено точно на approved source SHA `fd17f4346159234c8165c2b808b5c16309076e2e`.
- Stable GitHub Release `v1.2.5` опубліковано як не-draft і не-prerelease (Release ID `386734323`). Public asset `BDO-UA-Client-v1.2.5-win-x64.zip` (asset ID `556244252`, `67,905,838` bytes) має SHA-256 `e505971b3ed7946d085ce1fe169c9ab45cd73689f49c71393ab49ca65e7e8b7d`; internal EXE SHA-256 `f0a89d1348e66fa2cadae41d37a8c945282bb7753737d0593d1a8114df7cf5e3`.
- Public ZIP завантажено повторно; чотири flat-файли, manifest schema 2, version metadata і `SHA256SUMS.txt` збігаються. Release body використано зі structured `NEXT.json`.
- Створено `docs/releases/v1.2.5.md`, а `docs/releases/NEXT.json` скинуто до порожнього schema-v1 джерела. v1.2.5: **RELEASED / PUBLIC VERIFIED / PENDING EXTERNAL REVIEW**.
- Наступна дія: `External Architect final release review, then WORK CYCLE COMPLETE / OWNER DECISION REQUIRED`.

## 2026-09-12 · Stage 1 — explicit BDO game boundary

- На основі завершеного architecture audit створено один compact PRIMARY roadmap `game-boundary-refactoring` з двома stages: explicit game boundary та майбутня per-game persistence isolation. Game B, feed/API adapter і generic plugin framework не входять у scope.
- Concrete `BdoGameDefinition` тепер володіє BDO identity, localization target/validation, Steam/registry detection facts і composition `ads_files` patch reader. MainForm, GameDetector, localization state/install/restore та BackupStore використовують цей boundary; `ads/languagedata_en.loc`, storage layout/schema, API contract і runtime behavior не змінювалися.
- Validation: Release build — 0 warnings / 0 errors; focused boundary/detection/install/restore/lifecycle suite — 216 passed; full Release suite — 940 passed / 0 failed / 0 skipped; `git diff --check` passed.
- Stage 1: **IMPLEMENTED / VALIDATED / PENDING EXTERNAL REVIEW**. Stage 2: **NOT STARTED**. Stable release залишається `v1.2.5`; `NEXT.json` не змінювався.

## 2026-09-12 · Stage 2 — per-game persistence isolation

- Stage 1 `game-boundary-refactoring` externally accepted; Stage 2 authorized as a high-risk persistence/data-migration change.
- Added canonical `games/<stable-game-id>/` ownership for game config, installation state and localization backups; application config, logs, cache and self-update sessions remain application-global.
- Added bounded, idempotent legacy BDO migration using same-volume moves. Canonical scoped state remains authoritative on conflicts; malformed, unreadable or semantically null legacy config is logged/preserved while independent state/backups migration continues, and filesystem migration failures remain fatal/fail-closed without destructive cleanup.
- JSON formats, backup contents, physical localization target, API contract, UI and transaction semantics remain unchanged. Added focused isolation/migration coverage; no Game B or generic storage framework.
- Validation: focused migration/isolation suite — 15 passed; relevant install/restore/detection/state/MainForm lifecycle suites — 170 passed; Release build — 0 warnings / 0 errors; full Release suite — 957 passed / 0 failed / 0 skipped; `git diff --check` passed. MainForm off-screen background restore regression coverage and native secondary-activation validation passed.
- Stage 2: **IMPLEMENTED / VALIDATED / PENDING EXTERNAL PRE-COMMIT REVIEW**. Commit, push and CI are intentionally deferred until external Architect acceptance.

## 2026-09-14 · v15.59 — game-boundary-refactoring closure

- External Architect accepted the exact Stage 2 pre-commit patch; implementation commit `81823e6` (`v15.59 — ізолювати persistent state за грою`) was pushed to `main`.
- Normal CI #202 / run `34841029682` succeeded for the exact implementation SHA. Stage 2 is now **REVIEWED / ACCEPTED**; Stage 1 remains **REVIEWED / ACCEPTED**.
- `game-boundary-refactoring` was moved to `docs/plans/archive/` and marked **COMPLETED / ARCHIVED**. No Stage 3, Game B, feed abstraction or follow-up refactoring was created.
- Stable public release remains `v1.2.5`; release files and `NEXT.json` were not changed. Repository enters `WORK CYCLE COMPLETE / OWNER DECISION REQUIRED` with no ACTIVE PRIMARY roadmap.

## 2026-09-14 · v1.2.6 release candidate preparation

- Підготовлено schema-v1 `docs/releases/NEXT.json` для `v1.2.6` з user-facing описом game-scoped persistence migration та відновлення background/off-screen MainForm.
- Stable public release залишається `v1.2.5`; production code, workflows, release tag і GitHub Release не змінювалися.
- Наступна дія: Release Candidate `v1.2.6`, потім Owner native UI smoke exact artifact. До Owner acceptance реліз не вважається опублікованим.

## 2026-09-14 · v1.2.6 release candidate ready

- Release-prep commit `daba3b6aa861f072b382a3b1f92a24d52b94763f` пройшов normal CI #204 / run `34873696880`; Release Candidate #34 / run `34873972036` успішно зібрано з цього exact source SHA.
- RC artifact `BDO-UA-Client-v1.2.6-win-x64` (artifact ID `10361150176`) містить чотири flat-файли; ZIP має `67,914,367` bytes і SHA-256 `cf999c7059c8c1eecb2ff68053feb59410c30c3956a1a8e214bd078240c1c435`; internal EXE SHA-256 `65dfb3d2e76e011e256e3d6f6c7d27994a8ac54f89d6e64dbb86e9e9328fcd35`; manifest schema 2, version `1.2.6`, commit відповідає release-prep SHA.
- Repository state: `v1.2.6 — RC READY / OWNER NATIVE SMOKE REQUIRED`; final tag і public GitHub Release навмисно не створені. Наступна дія — Owner native UI smoke exact RC artifact, потім final tag/publication.

## 2026-09-14 · v1.2.6 stable release publication

- Owner native UI smoke exact RC #34 / run `34873972036` прийнято; annotated tag `v1.2.6` створено точно на approved source SHA `daba3b6aa861f072b382a3b1f92a24d52b94763f`.
- Stable GitHub Release `v1.2.6` опубліковано як не-draft і не-prerelease (Release ID `388677576`). Public asset `BDO-UA-Client-v1.2.6-win-x64.zip` (asset ID `564129830`, `67,914,367` bytes) має SHA-256 `cf999c7059c8c1eecb2ff68053feb59410c30c3956a1a8e214bd078240c1c435`; internal EXE SHA-256 `65dfb3d2e76e011e256e3d6f6c7d27994a8ac54f89d6e64dbb86e9e9328fcd35`.
- Public ZIP завантажено повторно; чотири flat-файли, manifest schema 2, version metadata і `SHA256SUMS.txt` збігаються. `docs/releases/v1.2.6.md` створено, а `docs/releases/NEXT.json` скинуто до порожнього schema-v1 джерела.
- Repository lifecycle: `v1.2.6 — RELEASED / PUBLIC VERIFIED / PENDING EXTERNAL RELEASE REVIEW`. Наступна дія: external Architect final release review, потім `WORK CYCLE COMPLETE / OWNER DECISION REQUIRED`.

## 2026-09-17 · v15.64 — compact multi-mode window minimum

- Для трьох і більше localization modes MainForm тепер використовує компактний minimum client width `820px`, достатній для трьох карток із їх мінімальною шириною та проміжками; для одного/двох режимів збережено `960px` для global status UI.
- Column thresholds layout-у тепер обчислюються від фактичної мінімальної ширини картки, без зміни install/update/restore behavior.
- Додано MainForm lifecycle regression coverage для трьох карток; оновлено UI/release context. Статус: **IMPLEMENTED / VALIDATED / PENDING EXTERNAL REVIEW**.

## 2026-09-17 · v15.64 corrective — preserve compact width during mode rebuild

- Виправлено transient rebuild path: мінімальна ширина більше не визначається за тимчасово очищеним `modesFlowPanel`; для 3+ installable modes компактний minimum зберігається під час semantic feed rebuild, а loading/failure/no-mode placeholders повертаються до global minimum.
- Додано lifecycle coverage на фактичній компактній ширині з повторним 3-mode feed rebuild; v1.2.6 має статус **RELEASE REVIEWED / ACCEPTED**. Поточний v15.64: **IMPLEMENTED / VALIDATED / PENDING EXTERNAL REVIEW**; Owner visual smoke залишається окремим gate.

## 2026-09-17 · v15.67 — prepare Release Candidate v1.2.7

- v15.64/v15.65 отримали external Architect acceptance та Owner visual smoke acceptance.
- Для patch release `v1.2.7` підготовлено structured release notes про compact three-mode layout; stable public release залишається `v1.2.6`.
- Наступний крок — canonical Release Candidate workflow для exact preparation SHA; final tag і public release не створюються на цьому етапі.

## 2026-09-17 · v1.2.7 — RC READY

- Release Candidate #35 / run `35270372972` успішний для exact source SHA `b21118d1ec8e6d0342fb0e47544e911fcb875739`; artifact ID `10517654612`, `BDO-UA-Client-v1.2.7-win-x64`.
- Перевірено чотири flat-файли, EXE version `1.2.7` / FileVersion `1.2.7.0`, EXE SHA-256 `c0ed6221aa4e9a776ea23ed0e500a96dc7ab4a5386f1b55cc8e6b9ecbebd3c28`, ZIP size `67,914,379` bytes, ZIP SHA-256 `f05cc7f3bcf8a9f5eae89e58bfb6196e36d4bc4097082fcc276f0f83d05dd13c`; manifest schema 2 і `SHA256SUMS.txt` збігаються.
- Стан: `v1.2.7 — RC READY / OWNER NATIVE SMOKE REQUIRED`; final tag/public release не створені. Наступна дія — Owner native smoke exact RC.

## 2026-09-18 · Localization Hub / Multi-game Foundation — BACKLOG

- Owner затвердив майбутній напрям перетворення застосунку на `Хаб українізаторів` — Windows launcher/hub для кількох незалежних локалізаційних ігор; BDO залишається єдиною реалізованою production-грою на початку.
- Створено compact BACKLOG roadmap `localization-hub-multigame`; implementation authorization відсутня. Реальний контракт другої гри ще очікується.
- Майбутнє перейменування GitHub repository на `merelyigor/ua-localization-hub` є окремим bridge-gated Owner operational stage; зараз не виконується. План очікує завершення v1.2.7 та явну активацію Owner.

## 2026-09-18 · v1.2.7 — PUBLIC VERIFIED

- Owner native smoke exact RC #35 / run `35270372972` прийнято; annotated tag `v1.2.7` створено точно на approved source SHA `b21118d1ec8e6d0342fb0e47544e911fcb875739`.
- Stable GitHub Release `v1.2.7` опубліковано як не-draft і не-prerelease (Release ID `391117230`). Public asset `BDO-UA-Client-v1.2.7-win-x64.zip` (asset ID `571358994`, `67,914,379` bytes) має SHA-256 `f05cc7f3bcf8a9f5eae89e58bfb6196e36d4bc4097082fcc276f0f83d05dd13c`.
- Public ZIP завантажено повторно; чотири flat-файли, manifest schema 2, version metadata, EXE SHA-256 `c0ed6221aa4e9a776ea23ed0e500a96dc7ab4a5386f1b55cc8e6b9ecbebd3c28` і `SHA256SUMS.txt` збігаються з approved RC.
- Release lifecycle: `v1.2.7 — RELEASED / PUBLIC VERIFIED / PENDING EXTERNAL RELEASE REVIEW`. `NEXT.json` скинуто до порожнього schema-v1 джерела; roadmap `localization-hub-multigame` залишається BACKLOG.

## 2026-09-18 · Stage 0 — activate Localization Hub roadmap

- `v1.2.7` отримав external release review: **RELEASE REVIEWED / ACCEPTED**; BLOCKER/IMPORTANT відсутні.
- Owner явно активував `localization-hub-multigame`; plan переміщено `BACKLOG → ACTIVE / PRIMARY`, Stage 0 завершено, production code не змінювався.
- Наступна й єдина authorized action — Stage 1 `Hub shell + selected-game model`.

## 2026-09-18 · v15.72 — Stage 1 hub shell and selected-game model

- Реалізовано bounded Stage 1: explicit compile-time `GameCatalog` з єдиною production-грою `black-desert-online`, optional `selected_game_id` у global application config та BDO fallback/repair без destructive migration.
- MainForm отримав shell `Хаб українізаторів`, generic subtitle і disabled native selector `Black Desert Online`; runtime game switching, Game B і Stage 2 session boundary не реалізовувалися.
- Додано focused catalog/config/MainForm lifecycle coverage, оновлено storage/UI/architecture/release context. Статус: **IMPLEMENTED / VALIDATED / PENDING EXTERNAL REVIEW + OWNER VISUAL SMOKE**.

## 2026-09-18 · Stage 1 accepted / Stage 2 pre-commit implementation

- External Architect review і Owner visual smoke для Stage 1 прийняті: Stage 1 — **REVIEWED / ACCEPTED**.
- Реалізовано локальний Stage 2 bounded `BdoGameSession`, який володіє concrete BDO persistence/API/localization/feed runtime composition; runtime switching не реалізовувався.
- Stage 2 пройшов локальну валідацію та очікує **PRE-COMMIT EXTERNAL ARCHITECT REVIEW**. Commit, push і CI навмисно не виконані.

## 2026-09-18 · Stage 2 accepted and committed

- External pre-commit Architect review завершено: **PRE-COMMIT REVIEWED / ACCEPTED**, BLOCKER `0`, IMPORTANT `0`.
- Stage 2 runtime/session boundary закомічено та перевірено normal CI; persistent state оновлено до **Stage 2 — REVIEWED / ACCEPTED**.
- Наступна authorized фаза — Stage 3 `Safe game switching lifecycle`; Stage 3 ще не реалізований.

## 2026-09-18 · Stage 3 — safe game switching lifecycle

- Реалізовано bounded `SelectedGameSessionHost` для однієї активної concrete BDO session: switch блокується під час startup/операцій/закриття, стара session скасовується й drain-иться перед commit candidate.
- Додано session generation/cancellation guards, від'єднання feed handlers, reset transient monitor/notification state, cancellation-aware startup і synthetic A→B lifecycle coverage; production catalog як і раніше містить лише BDO.
- Локальна перевірка Stage 3: focused session/startup/poller/MainForm suite — 65 passed; full Release suite — 976 passed; Release build — 0 warnings / 0 errors; `git diff --check` пройшов.
- Stage 1 і Stage 2 залишаються **REVIEWED / ACCEPTED**; Stage 3: **IMPLEMENTED / VALIDATED / PENDING EXTERNAL REVIEW**. Stage 4 не розпочато.

## 2026-09-18 · v15.75 — Stage 3 corrective lifecycle coverage

- Виправлено initial activation persisted registered game: відомий `selected_game_id` тепер активує відповідну тестову session до startup load; production catalog і далі містить лише BDO.
- Додано deterministic MainForm coverage без `Task.Delay` для switch completion, A→B→A restoration, stale old-session feed rejection і selector blocking під час localization operation.
- Validation: full Release suite — 980 passed / 0 failed / 0 skipped; Release build — 0 warnings / 0 errors; `git diff --check` passed. Stage 3 залишається **IMPLEMENTED / VALIDATED / PENDING EXTERNAL REVIEW**; Stage 4 не розпочато.

## 2026-09-18 · v15.76 — Stage 4 per-game feed-cache isolation

- Release-feed cache ownership перенесено з application-global `{root}\cache\release-feed.json` у game-scoped `{root}\games\<stable-game-id>\cache\release-feed.json`; production catalog залишається BDO-only.
- Додано BDO-only best-effort import валідного legacy feed cache зі збереженням source; scoped cache authoritative, invalid/missing legacy cache не блокує startup, synthetic session не імпортує BDO legacy data.
- Validation: focused cache/path/migration/session/MainForm suites green; full Release suite — 988 passed / 0 failed / 0 skipped; Release build — 0 warnings / 0 errors; `git diff --check` passed. Stage 3 — **REVIEWED / ACCEPTED**; Stage 4 — **IMPLEMENTED / VALIDATED / PENDING EXTERNAL REVIEW**; Stage 5 не розпочато.

## 2026-09-18 · v15.77 — Stage 5 visible rebrand

- Application-facing identity updated to `Хаб українізаторів` across the active window shell, tray, notifications, autostart prompt, help/uninstall guidance, update helper, README, current documentation and generated release notes; Black Desert Online remains the selected production game.
- Technical compatibility identities remain unchanged: repository slug, `BDO-UA-Client.exe`, package names, updater target/User-Agent, autostart registry value and `%LocalAppData%\\BDO-UA-Client`.
- Stage 4 is **REVIEWED / ACCEPTED**; Stage 5 is **IMPLEMENTED / VALIDATED / PENDING EXTERNAL REVIEW + OWNER VISUAL SMOKE**. Stage 6 has not started.

## 2026-09-18 · v15.78 — Stage 6 technical identity bridge preparation

- Stage 5 external Architect review and Owner visual smoke are accepted. Active technical compatibility identities are centralized without changing the current EXE, ZIP, autostart value, User-Agent or `%LocalAppData%\\BDO-UA-Client` names.
- GitHub release discovery preserves `merelyigor/bdo-ua-client` as the first endpoint and tries `merelyigor/ua-localization-hub` only after legacy HTTP 404; other failures remain fail-closed. No repository mutation was performed.
- Stage 6 is **IMPLEMENTED / VALIDATED / PENDING PRE-COMMIT EXTERNAL ARCHITECT REVIEW**; Stage 7 has not started.

## 2026-09-18 · v15.78 — Stage 6 review accepted

- External pre-commit Architect review завершено: **PRE-COMMIT REVIEWED / ACCEPTED**, BLOCKER `0`, IMPORTANT `0`.
- Technical identities фізично не змінювалися; bridge-capable repository discovery прийнято. Stage 6 — **REVIEWED / ACCEPTED**.
- Наступна фаза — Stage 7: підготувати й перевірити bridge release; фактичне перейменування repository залишається окремим Owner operational gate і ще не розпочато.

## 2026-09-18 · Stage 7A — bridge RC v1.2.8 ready

- RC #36 / run `35353772206` успішний на exact accepted Stage 6 source SHA `7cb740d55bc7baeb5aa9f91365114e68bb88eff9`; artifact ID `10550809226`, `BDO-UA-Client-v1.2.8-win-x64`.
- Перевірено чотири flat-файли, EXE `1.2.8.0` / ProductVersion `1.2.8`, Hub product metadata, EXE SHA-256 `bb4ce07897b4ca916e746cf41071deddcd1a9e6daa50623bfba3b5785dfc33e4`, ZIP size `67,924,024` bytes і ZIP SHA-256 `33d4f7baacac5b5a38a28140295663e6b1f865b01231e8845e6b46f1f87bf6f2`; manifest schema 2, `SHA256SUMS.txt` та generated notes збігаються.
- Historical v1.2.7 updater structural preflight пройдено (59 focused tests). Стан: **BRIDGE RC READY / OWNER NATIVE SMOKE PENDING**; live v1.2.7 → v1.2.8 update можливий лише після publication. Repository rename не авторизовано.

## 2026-09-18 · Stage 7B — v1.2.8 public release

- Stable GitHub Release `v1.2.8` опубліковано на exact annotated tag, peeled to RC source SHA `7cb740d55bc7baeb5aa9f91365114e68bb88eff9`; Release ID `391585430`, published `2026-09-18T15:31:08Z`, stable/non-draft/non-prerelease.
- Public asset ID `572906634`, `BDO-UA-Client-v1.2.8-win-x64.zip`, `67,924,024` bytes, SHA-256 `33d4f7baacac5b5a38a28140295663e6b1f865b01231e8845e6b46f1f87bf6f2`; public re-download and internal four-file verification passed. EXE SHA-256 `bb4ce07897b4ca916e746cf41071deddcd1a9e6daa50623bfba3b5785dfc33e4`.
- Owner exact RC smoke remains accepted. Lifecycle: **RELEASED / PUBLIC VERIFIED / LIVE LEGACY UPDATE PENDING**. Owner must run live public v1.2.7 → public v1.2.8 self-update before any repository rename; rename is not authorized.

## 2026-09-18 · Stage 7 — repository rename operational evidence

- Owner accepted live public v1.2.7 → v1.2.8 self-update before rename. Existing repository `merelyigor/bdo-ua-client` was renamed to `merelyigor/ua-localization-hub`; repository ID `1332444174` and release/tag/history were preserved.
- Old repository/release URLs redirect correctly, old API endpoint exposes GitHub move behavior to repository ID `1332444174`, and the old v1.2.8 asset URL still returns the exact SHA-256 `33d4f7baacac5b5a38a28140295663e6b1f865b01231e8845e6b46f1f87bf6f2`. Local origin now points to the canonical new slug.
- Owner log evidence confirms shipping v1.2.8 after rename fetched 17 releases and reported no eligible newer release on two checks. Exact v1.2.7 after rename selected candidate v1.2.8. Historical update-session cleanup warnings are unrelated and non-blocking.
- First post-rename docs/CI commit is the next operational proof. Stage 7: **REPOSITORY RENAMED / BRIDGE VERIFIED / POST-RENAME REFERENCE NORMALIZATION PENDING EXTERNAL ARCHITECT REVIEW**. Stage 8 не розпочато.

## 2026-09-18 · Stage 7 — post-rename canonical repository normalization

- Canonical repository references were normalized to `merelyigor/ua-localization-hub`; `merelyigor/bdo-ua-client` remains only as the legacy redirected compatibility fallback. Physical EXE, ZIP, User-Agent, autostart and LocalAppData identities remain unchanged.
- Owner product targets are now explicit: Black Desert Online / BDO UA Translate is **INTEGRATED / AVAILABLE**; Where Winds Meet / Winds4UA (W4U) is **PLANNED / INTEGRATION PENDING**. No WWM runtime catalog entry, API, detection or install contract was added.
- Stage 7: **POST-RENAME NORMALIZATION IMPLEMENTED / VALIDATED / PENDING PRE-COMMIT EXTERNAL ARCHITECT REVIEW**. Stage 8A explicit supported-project UI and Stage 8B optional physical identity migration are not started; Stage 9 waits for technical WWM integration data.

## 2026-09-18 · Stage 7 — normalization pre-commit review accepted

- External Architect accepted the post-rename canonical repository normalization: **PRE-COMMIT REVIEWED / ACCEPTED**, BLOCKER `0`, IMPORTANT `0`.
- Commit, push and CI are authorized. Stage 8A and Stage 8B remain **NOT STARTED**; Stage 9 remains **WAITING ON TECHNICAL INTEGRATION DATA** for Where Winds Meet / Winds4UA (W4U).
- Next action after finalization is external final Architect review of the committed Stage 7 normalization.

## 2026-09-18 · Stage 8A — explicit supported-project UI

- Stage 7 post-rename canonical normalization отримала final external Architect acceptance. Stage 8A додав до головного вікна явне представлення Black Desert Online / BDO UA Translate як доступної інтеграції та Where Winds Meet / Winds4UA (W4U) як запланованої.
- Production `GameCatalog` залишається BDO-only; WWM не зареєстровано як runtime game і не додано API, detection, install чи restore contract. Stage 8B — optional/not started; Stage 9 очікує технічні integration data.
- Validation: Stage 8A presentation assertions разом із MainForm lifecycle/GameCatalog — 29 passed; release-note generator — 24 assertions passed; Release build — 0 warnings / 0 errors; full Release suite — 997 passed / 0 failed / 0 skipped; `git diff --check` пройшов. Стан: **IMPLEMENTED / VALIDATED / PENDING EXTERNAL ARCHITECT REVIEW / OWNER VISUAL SMOKE PENDING**.

## 2026-09-18 · Stage 8A acceptance

- External Architect review Stage 8A прийнято: BLOCKER `0`, IMPORTANT `0`. Owner native/visual smoke також прийнято; BDO UA Translate показується як `Доступно`, Winds4UA (W4U) — як `Інтеграція готується`, без layout regression.
- Stage 8A — **REVIEWED / ACCEPTED**. Stage 8B залишається **OPTIONAL / NOT STARTED**, Stage 9 — **WAITING ON TECHNICAL INTEGRATION DATA**, Stage 10 — **NOT STARTED**. Подальший implementation stage не авторизовано; наступна дія — **OWNER DECISION REQUIRED**.

## 2026-09-25 · v15.85 — Stage 8C visible brand and adaptive height

- Видиму назву оновлено на «Хаб Українізаторів BDO - WWM»; підзаголовок прямо називає BDO та WWM. Рядки BDO UA Translate / Winds4UA збережено, runtime catalog лишився BDO-only.
- Підгонка висоти тепер може зменшувати й збільшувати вікно, враховує мінімальну клієнтську висоту та робочу область екрана; при переповненні лишається вертикальне прокручування. Вертикальне перетягування вікна не запускає повторну підгонку.
- Validation: adaptive-height policy — 6 passed; MainForm lifecycle/layout — 23; GameCatalog — 6; SelectedGameSessionHost — 2; combined focused set — 37; release-note generator — 24 assertions; resolver — 13; Release build — 0 warnings / 0 errors; full Release suite — 1003 passed / 0 failed / 0 skipped. Stage 8C — **IMPLEMENTED / VALIDATED / PENDING EXTERNAL ARCHITECT REVIEW / OWNER VISUAL SMOKE PENDING**.
- Stage 8B лишається **OPTIONAL / NOT STARTED**; Stage 9 — **WAITING ON TECHNICAL INTEGRATION DATA**; Stage 10 — **NOT STARTED**.

## 2026-09-25 · v15.86 — Stage 8C placeholder-height correction

- Виправлено перерахунок секції локалізації для loading, failure та нульової кількості доступних режимів: висота враховує preferred height тексту, margins/padding і caption, а повернення карток повторно розширює секцію. Form-level content fitter і прийняті brand/layout зміни не змінювалися.
- Детерміновані MainForm-перевірки покривають переходи дворядні картки → короткий стан → картки та звичайне вертикальне resize без повторного content-fit циклу. Stage 8C залишається **IMPLEMENTED / VALIDATED / PENDING EXTERNAL ARCHITECT REVIEW / OWNER VISUAL SMOKE PENDING**.

## 2026-09-25 · Stage 8C acceptance

- External Architect accepted Stage 8C: BLOCKER `0`, IMPORTANT `0`. Owner native/visual smoke accepted the exact brand/subtitle, BDO/Winds4UA project rows, adaptive content height, tray minimize/restore, application exit and usable BDO controls/cards. The accepted implementation includes the empty/loading/failure mode-section height correction.
- Stage 8C is **REVIEWED / ACCEPTED**. Stage 8B remains **OPTIONAL / NOT STARTED**; Stage 9 remains **WAITING ON TECHNICAL INTEGRATION DATA** for Where Winds Meet / Winds4UA (W4U); Stage 10 remains **NOT STARTED**. No further implementation work is authorized; next action: **OWNER DECISION REQUIRED**.

## 2026-09-25 · v15.88 — v1.2.9 Release Candidate prepared

- Release Candidate #37 / run `36166009527` succeeded from exact frozen application source SHA `5c2c3a71fac3f0b57405c3494db4327f2a02295d`; empty version input resolved to `1.2.9` / `v1.2.9`. Artifact ID `10878006746`, `BDO-UA-Client-v1.2.9-win-x64`, 67,905,336 bytes, outer SHA-256 `c43ae054a3a349158607d5de69f923df216a8f4db9ade06c61545e178400b298`.
- Exact bundle verification passed: four flat files; EXE 162,466,846 bytes, SHA-256 `610329b4b59eef49092a9a721d88e4b4919aff914764c82cd2221d3584f9910d`, FileVersion `1.2.9.0`, ProductVersion `1.2.9`, Hub ProductName/FileDescription; schema-2 manifest, SHA256SUMS and generated notes matched the frozen source/run and current `NEXT.json`.
- Lifecycle: **v1.2.9 RC READY / OWNER NATIVE SMOKE PENDING**. Proposed final tag target remains source SHA `5c2c3a71fac3f0b57405c3494db4327f2a02295d`, not the later docs-only evidence commit. No tag/release was created; production publication is not authorized and `NEXT.json` was not reset.
